# 王者峡谷 PvE · 关卡系统设计文档

> 版本 v1.1 · 2026-09-22 · 状态：**待 Paper Review**
> 读者对象：实现者。目标：30 分钟读完即可开工。
> 数值标注规则：✅ 有 rationale（含推导）｜`[PLACEHOLDER]` 已填占位值 + 假设 + 验证路径｜⚗️ 必须 playtest 标定后才准进正式 build
>
> **基线保鲜条款**：§2 现状基线对应 game.js commit `775f22b`。核心数值（伤害/射速/移速/散布/血量）
> 每次代码变更须同步 §2——**基线过期 = 本文档所有推导作废重查**。P4 管代码里的数字，这条管文档里的数字。

---

## 1. Fun Hypothesis 与 Design Pillars

**Fun Hypothesis**：这游戏好玩的核心是 **「在越来越密的压力下，始终清楚下一秒该先杀谁」**——目标优先级决策带来的心流，而不是单纯的枪法。

**Design Pillars**（之后每个设计决策都用这四条过审）：

| # | 支柱 | 含义 | 反例（违反即砍） |
|---|------|------|------------------|
| P1 | **每关只教一件事** | 新怪/新枪/新场地引入关，其余变量保持克制 | 一关同时塞新怪+新场地+数值暴涨 |
| P2 | **压力可读，死的明白** | 每个伤害来源必须有预警（吟唱/抬臂/体型），玩家阵亡能说出"我该先杀那个" | 任何无前摇的伤害 |
| P3 | **3-5 分钟一关** | 碎片时间可完成一局；通不了关的原因是决策不是数值墙 | 同一场战斗超过 6 分钟 |
| P4 | **数值必须能讲出为什么** | 每个数字后面挂着推导或 `[PLACEHOLDER]`，禁止 magic number | "先填个 999 跑跑看" |

---

## 2. 现状基线（从 game.js 实测，设计的出发点）

| 项 | 数值 | 出处 |
|---|---|---|
| 玩家 HP | 100，**无局内回血** | `PLAYER_MAX_HEALTH` |
| AK-47 | 单发 34 / 爆头 ×2 / 射速 0.12s/发 / 弹匣 30 / 换弹 1.5s | `BASE_DAMAGE` / `shootCooldown` |
| 红怪（近战） | 150 HP / 伤害 25 / 追速 5.0 / 停步 1.8m | `maxHealth` / `RED_MELEE_DAMAGE` |
| 蓝怪（远程施法） | 100 HP / 伤害 10 / 射程 12m / 吟唱 0.4s / 冷却 1.5s | `BLUE_CAST_*` |
| 单局刷怪 | 6~8 只同场，杀光即胜利 | `spawnMonsters(6~8)` |
| 计分 | 命中 10 / 爆头额外 25 / 击杀 100 | `SCORE_PER_*` |
| 评级 | 二值：胜利 S / 阵亡 F | `endGame()` |
| 场地 | x∈[-26,26], z∈[-46,12]，9 个碰撞障碍 | `solidObstacles` |
| **弹道散布（两层）** | 站桩精准；移动 inaccuracy（开火移速 2.4 → 0.15 rad，10m 散布半径 ≈1.5m）+ 空中 ×2.5 + 水平后坐固定 spray pattern（10 发循环） | `SPREAD_*` / `SPRAY_YAW_PATTERN` |
| **穿墙遮挡** | 子弹射线被障碍 AABB 拦截（含高度门控），隔墙不判伤 | `rayHitObstacleDistance` |

**关键推导锚点（后续所有曲线以此为基准）：**

- **玩家有效 DPS ≈ 125（取整 120 用）** ✅：完整推导链——
  循环上限 200（30 发 × 34 ÷ 5.1s 循环，含 1.5s 换弹）
  × 命中率 50% ⚗️（移动靶 + 散布系统修正）
  × 爆头伤害系数 1.25 ⚗️（假设爆头率 25% → 均伤 42.5/34）
  = **125**。原「→ 120」漏写爆头一步，违反 P4，已补全。
  - **敏感性**：命中率每 ±10pp → DPS ±25 → 全文血池 ∓20%。M0 标定的输出是**一个血池缩放系数**，不是逐格改表。
  - ⚗️ 爆头率 25% 是 DPS 与 §6 S 档阈值的共同分母，同样进 M0 标定（§12）。
- **玩家容错 = 红 4 掌 / 蓝 10 发** ✅：100 HP 无回血，这是每关「压力预算」的分母。

---

## 3. 元循环三层钩子

```
Moment(0-30s)   看到怪 → 判断优先级 → 射击/走位 → 命中反馈（血条/闪烁/音效）
Session(3-5min) 进入关卡 → 压力递增 → 杀光/Boss 倒地 → 评级结算 → 解锁下一关
Long-term(小时) 通关 L7 + 每关刷 S 评级 + 收集 3 把武器
                        ↳ localStorage 持久化，刷新不丢
```

**长期钩子的边界（明确不做）**：不做金币/商店（多一个货币就要回答"买什么"的新决策链，且 source 没有对应 sink，通胀预定）；不做天赋树（与"枪法+决策"的核心无关）。想加是另一个项目。

---

## 4. 关卡总表（7 关，线性解锁）

| 关 | 名称 | 教什么（P1） | 怪物配置 | 场地 | 血池 | 纯输出时长* | 过关奖励 |
|---|---|---|---|---|---|---|---|
| L1 | 峡谷初见 | 移动/射击/换弹/疾跑脱战 | 红×4 | 现有布局 | 600 | 5.0s | 解锁 L2 |
| L2 | 蔚蓝威胁 | **远程优先级**：先杀吟唱中的蓝怪 | 红×3 蓝×3 | 现有布局 | 750 | 6.3s | 解锁 L3 |
| L3 | 野区窄道 | 窄位拉扯，别把怪拉成一团 | 红×4 蓝×3（血/伤 ×1.1） | **变体 A** | 990 | 8.3s | 解锁 L4 + **霰弹枪** |
| L4 | 蟹群来袭 | **目标切换**：贴脸蟹群 vs 远处施法 | 蟹×4 红×3 蓝×2 | 变体 A | 922 | 7.7s | 解锁 L5 |
| L5 | 暗影先锋 | **集火精英**：爆头价值放大 | 精英红×1 红×3 蓝×3 | **变体 B** | 1050 | 8.8s | 解锁 L6 + **射手步枪** |
| L6 | 风暴前夕 | 全怪种混编高压（毕业考） | 精英红×1 精英蓝×1 红×3 蓝×3 蟹×3（血/伤 ×1.2） | 变体 B | 1665 | 13.9s | 解锁 L7 |
| L7 | 主宰降临 | Boss 机制学习：躲、清、打 | **主宰**（6000 HP，三阶段）+ 阶段 adds | 现有布局 | 6000+adds | ≈33s 对 Boss | 通关 + 自由重玩 |

\* 纯输出时长 = 血池 ÷ 有效 DPS 120，只含开枪时间，不含追逐拉扯。预估单关实机时长 = 该值 ×6~10（⚗️ playtest 标定 P3 是否成立）。

**难度曲线形状**：L1→L3 缓坡（建立信心），L4 第一个尖峰（新怪种+新枪同时给，用新工具解压），L5 回落蓄力，L6 陡峰（毕业考），L7 机制峰（考学习不考数值）。

### 4.1 新机制：同场上限 + 补位队列

- **Purpose**：L6 总数 11 只若同场，压力超出"可决策"上限（P2），且性能无必要承压。
- **规则**：同场活跃怪 ≤ **8** `[PLACEHOLDER]`；超出进 FIFO 队列，每有 1 只死亡动画**播完**（`removeMonster` 时机，与现有约定一致）补 1 只，入场点距玩家 ≥15m。
- **P2 合规条款**：入场点若落在玩家背后 90° 视锥内，必须先播 **1s 生成光柱预警**再可行动。否则「背后补位 → 落地即冲刺」= 无前摇伤害，直接违反 P2。
- **HUD**：剩余敌数 = 场上存活 + 队列（向玩家坦诚总账）。
- **Edge case**：队列补位触发时不打断任何进行中行为；Boss 关 adds 不走队列（Boss 阶段逻辑自管）。

---

## 5. 逐轴详细设计

### 5.1 数值递增轴

| 关卡 | 血量倍率 | 伤害倍率 | 追速倍率 | Rationale |
|---|---|---|---|---|
| L1-L2 | ×1.0 | ×1.0 | ×1.0 | 基线，教学期不动数值 |
| L3 | ×1.1 | ×1.1 | ×1.0 | 配合窄道场地，压力 +10% 可感知但不突变 |
| L4-L5 | ×1.1 | ×1.1 | ×1.0 | 新怪种是主角，旧怪数值保持 |
| L6 | ×1.2 | ×1.2 | ×1.05 | 毕业考，全面 +20%，速度 +5% 压缩风筝空间 |
| L7 | Boss 独立 | — | — | 见 5.3 |

- 追速刻意压到 ×1.05 上限 ✅：红怪 5.0×1.05=5.25 仍低于疾跑 7.2，保证"疾跑一定能脱战"这条底层承诺不破（否则 P2 崩溃）。
- 倍率落在配置表，不写死在创建函数里（见 §8 配置 schema）。

### 5.2 场地变化轴（2 套变体，不改碰撞系统）

复用现有 `registerSolid` 自登记机制——**布局即数据**：

- **变体 A「野区窄道」**：中路加 2 道横隔断，形成 3 条宽 2.5-4m 的走廊。⚠️ 走廊净宽必须 ≥0.8m 玩家直径 + 转弯余量（遵守现有间距约束，`registerSolid` 的 warn 会在摆放阶段拦截）。
- **变体 B「开阔祭坛」**：撤掉中央隔断，柱阵改为 2×3 对称双柱阵（精英/Boss 战风筝位）。
- **配置格式**：每关一个 `obstacles: [{type:'divider'|'pillar', x, z, w, d, h}, ...]` 数组，加载关卡时清场重建。
- **Edge case**：重建时必须同步清空 `solidObstacles` 并释放旧 geometry/material（与 `removeMonster` 释放约定同级）。

### 5.3 新怪种轴（3 种，全部程序化建模，零外部素材）

#### 迅捷蟹（L4 引入）— 「瞄准纪律的破坏者」

| 项 | 值 | Rationale |
|---|---|---|
| HP | 68（2 枪） | 让"打它值不值"成为真决策：2 发子弹换一片清净 |
| 速度 | 8.0 直线 / 转向角速度 ≤120°/s | 比疾跑 7.2 快→跑不掉；转向钝→横向滑步能甩（弱点可读，P2） |
| 伤害 | 5 / 扑击，冷却 2s | 单只刮痧，4 只围上来每秒 10——威胁来自数量 |
| 体型 | 0.6m 宽，贴地 | 视觉上明确"低头打"的新目标轴 |
| 行为 | Z 字突进 → 扑击 → 后跳拉开 | 三段都要有位移预警（后跳是节奏窗口） |

#### 精英石像（L5 引入）— 「集火与爆头的考卷」

- 红/蓝各自的强化版：HP ×2（精英红 300 / 精英蓝 220），伤害 ×1.5，体型 ×1.3，魔纹改紫、宝石加大。
- 追速 ×0.9 ✅：精英是"压迫型坦克"不是"刺客"，速度降一档让玩家有风筝解（P2），但血池厚到风筝成本很高——这就是它制造的决策。
- 同场 ≤1 只 ✅：两只精英同时存在会把"先杀谁"变成"被谁撵"，决策变混沌。

#### Boss 主宰（L7）— 三阶段机制怪

| 项 | 值 | 说明 |
|---|---|---|
| HP | 6000 `[PLACEHOLDER]` | 推导：命中率对大体型上调至 65% → 有效 DPS ≈184 → 纯输出 33s；+3 阶段转场与清 adds，目标总时长 2.5-3.5min（P3）。⚗️ 首战若 <2min 或 >4.5min 则调整 |
| 阶段 | P1 100-70%：近战拍击（红怪同款放大）；P2 70-40%：+ 法球三连（蓝怪吟唱放大，0.8s 预警线）；P3 40-0%：+ 召唤蟹×3（每 20s 一波） | 每阶段引入一个已教过的机制的放大版——Boss 是毕业综合考，不教新东西（P1） |
| 转场 | 每破阶段：2s 无敌 + 吼叫震屏 + **玩家回 30 HP** | `[PLACEHOLDER]` 回血是妥协：无局内回血下 Boss 战总伤池远超 100，不给补给=要求零失误，与休闲定位冲突。⚗️ 若实测玩家普遍满血进 P3，降到 20 |
| 失败信号 | 玩家阵亡 → 回 L7 起点，**不清已解锁进度** | 重复挑战成本 = 一关时长，不是全部人生 |

### 5.4 武器解锁轴（2 把新枪，程序化低模，登记进 `SFX_FILES`/`WEAPON_SFX` 双配置）

| | AK-47（现有） | 霰弹枪「裂空」（L3 通关） | 射手步枪「穿云」（L5 通关） |
|---|---|---|---|
| 单发伤害 | 34（爆头×2） | 9×8 弹丸（每丸独立判定，爆头不加成） | 85（爆头×2.5=212） |
| 射速 | 0.12s | 0.8s | 0.5s |
| 弹匣/换弹 | 30 / 1.5s | 6 / 2.2s | 8 / 1.8s |
| 射程衰减 | 无 | 12m 内全伤，20m 外≈0 | 无 |
| 定位 | 万金油 | **蟹群/贴脸的答案** | **精英/Boss 的答案** |
| 代价 | — | 远程无力、弹药经济差 | 近战泼水无力、容错低 |

- **设计原则** ✅：两把新枪都是"侧向展开"不是"上位替代"——AK 在任何关都不是错解，但特定场景新枪明显更优。若 playtest 出现"解锁后 AK 再没人用"，削新枪而不是 buff AK。
- **切枪规则**：桌面数字键 1/2/3，移动端 HUD 加切枪按钮；切枪 0.4s（无伤害窗口，不能用来取消后摇）；**切枪打断换弹**，弹药按现有 `RELOAD_MAG_IN_AT` 已判定状态结算（插弹匣前切走 = 白换）。未解锁键位置灰。
- **继承约定**：音效走 `WEAPON_SFX` 槽位映射，采样缺失回退合成音；后坐力/散布/弹道轨迹复用现有框架，新枪只改参数表。

---

## 6. 评级系统（S/A/B/F，替代现有二值 S/F）

| 档 | 条件（全部从 state 现成统计计算） |
|---|---|
| **S** | 命中率 ≥ **标定中位数 + 5pp** ⚗️（M0 前暂按 55% 占位）**且** 爆头率 ≥25% **且** 未死亡 **且** 用时 ≤ 关基准 ×1.0 |
| **A** | 命中率 ≥45% 且 未死亡 |
| **B** | 通关（保底） |
| **F** | 阵亡 |

- **S 档阈值的立场**：故意高于设计基准（50%）——S 是「刻意追求」不是「认真玩的常态」，与 A 拉开档距。
- ⚠️ **已知张力（记账，M0 后决策）**：散布系统下高命中率偏好站桩输出，与 P1 教的「走位拉扯」存在激励冲突。候选处置：① S 附加条件改为「命中率 × 移动距离加权」；② 接受 S = 站桩流派。二选一，不许悬空。
- **时间基准公式** ✅：`parTime = 纯输出时长 × 10（P3 区间上沿）× 1.5（宽松系数）`。例：L1 = 5.0 × 10 × 1.5 = **75s**。初版按公式生成，playtest 后按 P75 玩家用时收紧。⚗️
- Rationale ✅：S 要求"快+准+稳"三合一，A 只要求准和稳。若 ⚗️ S 达成率 >40%，阈值上调 5-10%。
- 结算屏现状（score/kills/headshots/accuracy/combo）不动，只把评级字母逻辑换掉。

---

## 7. 存档与解锁（localStorage）

```json
// key: "wk.pve.save.v1"
{
  "version": 1,
  "unlocked": 3,
  "weapons": ["ak47", "shotgun"],
  "levels": {
    "1": { "bestGrade": "A", "bestScore": 4200, "clears": 3 },
    "2": { "bestGrade": "S", "bestScore": 5100, "clears": 2 }
  }
}
```

| 规则 | 说明 |
|---|---|
| 写入时机 | **仅过关结算时**（unlock 下一关 + bestGrade/bestScore 取优 + clears+1 + 发武器）；阵亡不写盘 |
| 读取失败 | JSON 解析异常 → 原值备份到 `wk.pve.save.v1.backup`，重置新档，HUD 角标提示"存档已重置" |
| 写入失败（隐私模式等） | 降级为内存档，当次会话正常玩，选关界面角标"存档不可用，进度不保留" |
| version 不匹配 | 同上备份重置（v1 无迁移逻辑，字段预留） |
| 通关 L7 后 | 全关可重玩刷 S/分数；`unlocked` 封顶 7 |

**选关 UI**：开始屏加「选择关卡」→ 7 张卡片（编号/名称/最佳评级徽章/最佳分数；锁定卡置灰 + 🔒 + "通关上一关解锁"）；结算屏加「下一关」直进按钮（过关且未到 L7）；暂停屏加「放弃本关」回选关。风格沿用现有峡谷 UI，移动端卡片两列。

---

## 8. 实现层约定（配置即数据）

### 8.0 架构决策：拆文件（M1 开工前置）

game.js 已 ~3500 行，关卡/武器/怪种/Boss/存档全塞进去会失控。零构建约束下**多文件原生 ES Module 完全可行**（importmap 已就位），M1 前先拆：

```
js/config/levels.js    — LEVELS 表（纯数据，schema 校验的输入）
js/config/weapons.js   — 武器参数表：伤害/射速/弹匣/换弹 + 散布/后坐/曳光参数全部字段化
js/monsters.js         — 怪种工厂（红/蓝/蟹/精英/Boss 共用 spawn 骨架，读 (type, mul)）
js/save.js             — localStorage 读写 + 备份降级（§7 规则的实现载体）
```

- **前置重构**：`SPREAD_PER_SPEED`、`recoilPerShot`、`TRACER_*` 等全局常量迁入武器表字段。§5.4「新枪只改参数表」目前**不成立**——霰弹的 inaccuracy 基数、弹丸数、射程衰减都需要 per-weapon 字段，这次重构是它的前提。
- 拆分只做「搬移 + export」，不改行为；以现有四套离线测试全绿为重构完成的判据。

### 8.1 LEVELS 配置表

新增 `LEVELS` 配置表，一关一行：

```js
const LEVELS = [
  { id:1, name:'峡谷初见', hpMul:1.0, dmgMul:1.0, spdMul:1.0,
    spawns:[{type:'red',count:4}], layout:'default',
    parTime: 75, maxAlive: 8, reward:{unlock:2} },   // parTime = 5.0×10×1.5，见 §6 公式
  // ... L2-L7 同构；L7 多一个 boss 字段
];
```

- 怪物创建函数改为读 `(type, mul)` 而非硬编码 150/100——**一次改动，旧逻辑全部走默认值**。
- `__SNAPSHOT__` 扩展：`.loadLevel(n)` / `.saveRead()` / `.unlockAll()` / `.gradeFor(stats)`，探针脚本逐关自动验收。
- 静态检查照旧走 `.workbuddy/tests/esm-lint.js`（.mjs 副本），新增关卡配置 schema 校验进 esm-lint。

---

## 9. 系统交互矩阵

| 系统 A × B | 关系 | 说明 |
|---|---|---|
| 关卡 × 暂停/重开 | intended | 重开 = 重进当前关，配置重读 |
| 关卡 × 移动端 | intended | 选关卡片两列、切枪按钮进触控 HUD |
| 武器 × 换弹 | intended | 切枪打断换弹，按 `RELOAD_MAG_IN_AT` 结算（§5.4） |
| 补位队列 × 死亡动画 | **风险** | 必须等 `removeMonster` 才补位，否则 dying 怪占位导致同场超编——进测试用例 |
| 存档 × 武器解锁 | intended | 发武器与 unlock 同事务写入，避免"关解了枪没给" |
| Boss 回血 × 评级 | acceptable | 转场回血不影响"未死亡"判定（只记死不记伤） |
| 场地重建 × 碰撞 | **风险** | 清场必须同步清 `solidObstacles`，否则旧碰撞盒残留成隐形墙——进测试用例 |
| 蟹群 × 绕行算法 | **风险** | 蟹的转向钝是刻意弱点，不能套用现有 `findDetourCorner` 强绕行，否则弱点消失——蟹单独写简化避障 |

---

## 10. Playtest 设计与失败信号

**先定义坏掉长什么样，再开 playtest。**

| 级 | 信号 | 判定为失败 | 处置 |
|---|---|---|---|
| A（必须修） | L1 玩家死亡率 | >20%（教学关死人=引导失败） | 降 L1 数量到 3，查新手引导 |
| A | M0 标定命中率偏离 50% 超 ±10pp | 标定完成但 §4/§5 数值未重算 | **按 §2 敏感性公式重算血池后才准进 M1**，禁止两张皮 |
| A | 任何关卡无法通关（数值墙） | 连续 5 次尝试无进展 | 当场改配置，不过夜 |
| A | 存档丢失/错乱 | 任何一次 | 阻断发布 |
| B | 单关平均尝试次数 | >4 次（难度墙） | 该关血/伤倍率 -10% |
| B | 玩家不点杀吟唱蓝怪 | 观察 3 人无人优先打蓝 | L2 教学提示补强，不是削蓝怪 |
| B | AK 解锁新枪后使用率 | <30%（新枪上位替代） | 削新枪，保 AK 地位（§5.4 原则） |
| C | S 评级达成率 | >40%（太松）或 <5%（太紧） | 调阈值 ±5-10% |
| C | 单关实机时长 | 中位数 <2min 或 >6min | 违背 P3，调血池 |

## 11. 分期路线图（每期结束都有能玩的 build）

| 期 | 内容 | 交付物 | 量级 |
|---|---|---|---|
| M0 | Paper review + 命中率/爆头率标定（`__SNAPSHOT__` 跑 3 局校准 §2 两个 ⚗️ 假设） | 校准后的血池缩放系数 + 数值表 | 0.5 天 |
| M0.5 | 架构拆分（§8.0：levels/weapons/monsters/save 四文件）+ 散布/后坐常量迁入武器表 | 行为不变的四文件结构，既有测试全绿 | 0.5 天 |
| M1 | 元循环骨架：`LEVELS` 配置 + 线性解锁 + localStorage + 选关 UI + S/A/B/F 评级（L1-L5 纯数值递增版） | 可完整闯关的骨架 | 1 天 |
| M2 | 场地变体 A/B + 布局配置化 + 清场测试 | 3 套场地 | 0.5 天 |
| M3 | 迅捷蟹 + 精英石像（AI/模型/平衡） | L4/L5 完整版 | 1.5 天 |
| M4 | 切枪框架 + 霰弹 + 射手步枪 + 音效登记 | 3 武器 | 1 天 |
| M5 | Boss 主宰 + L6/L7 + 补位队列 + S 档时间基准标定 | 全量内容 | 2 天 |

## 12. 待验证假设汇总

| # | 假设 | 若错了的影响 | 验证路径 |
|---|---|---|---|
| 1 | 玩家命中率 50% | 全文血池数值同比例缩放（§2 敏感性公式） | M0 用 `__SNAPSHOT__` 实测 3 局 |
| 1b | 爆头率 25%（DPS 系数 1.25 与 S 档阈值的共同分母） | DPS 推导与 S 档命中率阈值同时失准 | M0 与 #1 同批实测 |
| 2 | 单关实机 = 纯输出 ×6~10 | 关卡数量/血池重排 | M1 后掐表 |
| 3 | Boss 6000 HP → 2.5-3.5min | Boss HP 与转场回血联动调整 | M5 首战计时 |
| 4 | 同场上限 8 | 队列长度与节奏 | M5 压力测试 |
| 5 | 转场回血 30 | P3 难度感受 | M5 观察进 P3 血量分布 |

---

*本文档不含"上线后再调"。所有 `⚗️` 项在对应 milestone 内有明确标定动作；所有 `[PLACEHOLDER]` 已给默认值 + 验证路径。*
